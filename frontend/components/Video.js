import React from 'react'
import { useState } from 'react'
import styles from '../styles/Video.modules.css'

const Video = (
    key,
    address,
    url,
    channel,
    index,
    description,
    likeAddress,
    commentCount,
    likeVideo,
    createComment,
    getComments
) => {
    const [playing, setPlaying] = useState(false)

    return <div>Video</div>
}

export default Video
